---
import Section from '@/components/Section.astro'
---

<Section className="intro">
  <h2>
    I’m Christopher, <br class="mobile-breaks" />a product designer
    <br class="mobile-breaks" /> in the Pacific Northwest.
  </h2>
</Section>

<style>
  .intro {
    display: flex;
    flex-direction: column;
    justify-content: center;
    flex-grow: 1;

    @media (--viewport-phablet) {
      padding-bottom: 4rem;
    }

    @media (--viewport-tablet) {
      padding-bottom: 12rem;
    }
  }

  h2 {
    max-width: 14em;
    margin: 0;
    font-size: 2rem; /* 32px */
    font-family: 'EB Garamond', serif;
    font-weight: 500;
    letter-spacing: -0.5px;
    font-feature-settings: 'liga' 1;

    @media (--viewport-tablet) {
      font-size: 3rem;
    }
  }

  @media (--viewport-phablet) {
    .mobile-breaks {
      display: none;
    }
  }
</style>
